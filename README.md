# GMU Hackathon Backend

Simple Node.js (Express) backend service.

## Scripts

- `npm install` – install dependencies
- `npm run dev` – run in watch mode with nodemon
- `npm start` – run the server

The server exposes a basic health endpoint at `/health`.
